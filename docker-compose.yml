version: '3.8'

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

  # Uncomment when ready to run services in Docker
  # gateway:
  #   build:
  #     context: .
  #     dockerfile: packages/gateway/Dockerfile
  #   env_file:
  #     - .env
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - redis
  #   restart: unless-stopped

  # irc-bot:
  #   build:
  #     context: .
  #     dockerfile: packages/irc-bot/Dockerfile
  #   env_file:
  #     - .env
  #   depends_on:
  #     - redis
  #     - gateway
  #   restart: unless-stopped

volumes:
  redis-data:
